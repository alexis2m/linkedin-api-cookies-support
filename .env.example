# ------------------------------------------------------------------------------
# This is an example .env file.
#
# All of these environment vars must be defined either in your environment or in
# a local .env file in order to run the examples in this project.
# ------------------------------------------------------------------------------

LINKEDIN_EMAIL=
LINKEDIN_PASSWORD=

# Optional alternative to email/password auth: a raw cookie string from an
# authenticated LinkedIn browser session (must include `li_at` and `JSESSIONID`).
# LINKEDIN_COOKIES=

